arrays

example
 An array with 5 elements all 0 by default.
code
 var a [5]int
 a[4] = 100

example
 An array with 5 elments initialized.
code
 b := [5]int{1, 2, 3, 4, 5}

example
 A two dimensional array.
code
 var twoD [2][3]int
 for i := 0; i < 2; i++ {
     for j := 0; j < 3; j++ {
         twoD[i][j] = i + j
     }
 }
