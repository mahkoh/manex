#compdef manex
#generated by configure

typeset -A opt_args
AAA=($words[2,-1])
AAA=${(j:/:)AAA}
if [[ $words[-1] == '' ]]; then
    PRE=(/usr/local/share/manex/docs/$AAA/*)
else
    PRE=(/usr/local/share/manex/docs/$AAA*)
fi
for x in $PRE; do
    compadd $(basename $x)
done
